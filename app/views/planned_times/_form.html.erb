<%= form_for(@planned_time) do |f| %>
  <% if @planned_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planned_time.errors.count, "error") %> prohibited this planned_time from being saved:</h2>

      <ul>
      <% @planned_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hours %><br />
    <%= f.text_field :hours %>
  </div>
  <div class="field">
    <%= f.label :when %><br />
    <%= f.date_select :when %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :project %><br />
    <%= f.text_field :project %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
